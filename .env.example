# Dev Tunnel Proxy - example environment file
# Copy to .env and fill in values as needed

# ngrok
NGROK_AUTHTOKEN=
# Optional static domain (e.g., my-team.ngrok.app)
NGROK_STATIC_DOMAIN=

# Optional: Built-in AI assistant (proxied at /api/ai/*)
# Provide a key to enable endpoints
OPENAI_API_KEY=
# Chat model used for answers
OPENAI_MODEL=gpt-4o-mini
# Embedding model used for vector RAG indexing
OPENAI_EMBED_MODEL=text-embedding-3-small
