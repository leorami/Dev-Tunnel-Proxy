    # ngrok
    NGROK_AUTHTOKEN=
    NGROK_STATIC_DOMAIN=

    # OpenAI (optional)
    OPENAI_API_KEY=
    OPENAI_MODEL=gpt-4o-mini
    OPENAI_EMBED_MODEL=text-embedding-3-small

    # Dockerized Chrome (optional)
    CALLIOPE_PUPPETEER_IMAGE=ghcr.io/puppeteer/puppeteer:latest
    CALLIOPE_PUPPETEER_PLATFORM=linux/amd64

    # Local base for tools
    LOCAL_PROXY_BASE=http://dev-proxy

    # Dashboard API (optional)
    CALLIOPE_API=http://localhost:3001

    # Retention
    CONFIG_BACKUP_RETENTION=3
    ARTIFACTS_RETENTION_DAYS=14